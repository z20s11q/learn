# AGENTS.md

## Cursor Cloud specific instructions

This is a newly initialized repository (`learn`). As of the initial setup, the repo contains only a `README.md` placeholder — no application code, build system, dependencies, or services exist yet.

- **No services to start**: There are no backend, frontend, or database services.
- **No tests**: There are no test frameworks or test files.
- **No linting**: There are no linters configured.
- **No build**: There is no build system or compilation step.

Once application code is added, this section should be updated with relevant dev environment instructions (how to install deps, run services, lint, test, build, etc.).
