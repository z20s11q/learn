# AGENTS.md

## Cursor Cloud specific instructions

This is a scaffold repository ("learn") with no application code, dependencies, or services yet. The repo contains only a `README.md`.

- **No build/lint/test commands** are available — there are no source files or dependency manifests.
- **No services** need to be started.
- When application code is added in the future, update this file with relevant dev environment instructions (how to install deps, run, test, lint, etc.).
